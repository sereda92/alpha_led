import burger from './burger.js'

$(".one-time").slick({
    dots: true,
    infinite: true,
    speed: 1000,
    slidesToShow: 3,
    adaptiveHeight: true,
    autoplay: true,
    autoplaySpeed: 1000
  });
  
